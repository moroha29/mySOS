The price lists as they were in mysos.xlsx, for the tests that check the
quotation engine's sums against the workbook. The live lists in src/data are
edited from the website manager's Data tab, so tests must not depend on them.
