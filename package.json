{
  "name": "mysos-quotation-engine",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/prerender.mjs && vite build --mode mock",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "prerender": "node scripts/prerender.mjs",
    "dev:mock": "vite --mode mock",
    "icons": "node scripts/export-icons.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "exceljs": "^4.4.0",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "vitest": "latest"
  }
}
